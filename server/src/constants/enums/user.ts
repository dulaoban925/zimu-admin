/**
 * 用户模块枚举
 */

// 用户性别
export enum USER_SEX {
  MALE = 'male', // 男性
  FEMALE = 'female', // 女性
  UNKNOWN = 'unknown' // 未知
}

// 用户状态
export enum USER_STATUS {
  SERVING = 'serving', // 在职
  RESIGNED = 'resigned' // 离职
}
